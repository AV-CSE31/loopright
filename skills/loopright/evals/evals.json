{
  "name": "loopright",
  "version": "0.1.0",
  "cases": [
    {
      "id": "retry-loop-review",
      "prompt": "Use $loopright to review a retry loop that catches Exception forever.",
      "expected_behaviors": [
        "rejects unbounded retry",
        "requires transient failure allowlist",
        "requires budget and evidence"
      ]
    },
    {
      "id": "ml-tuning-contract",
      "prompt": "Use $loopright to design an Optuna tuning loop.",
      "expected_behaviors": [
        "defines objective metric",
        "sets trial or time budget",
        "requires baseline and final evidence"
      ]
    },
    {
      "id": "polling-deadline",
      "prompt": "Use $loopright to design a polling loop for a remote job.",
      "expected_behaviors": [
        "requires deadline or max polls",
        "handles terminal failure states",
        "requires cancellation or timeout evidence"
      ]
    },
    {
      "id": "async-bounded-concurrency",
      "prompt": "Use $loopright to review asyncio.gather over a very large input list.",
      "expected_behaviors": [
        "flags unbounded task creation",
        "requires a concurrency bound",
        "defines partial failure behavior"
      ]
    },
    {
      "id": "agent-repair-loop",
      "prompt": "Use $loopright to define a coding-agent repair loop for a failing test.",
      "expected_behaviors": [
        "sets inspect-edit-test cycle budget",
        "detects repeated failure",
        "requires deterministic completion evidence"
      ]
    },
    {
      "id": "loop-doctor",
      "prompt": "Use $loopright to diagnose and minimally repair a loop prompt that says keep trying until it works.",
      "expected_behaviors": [
        "returns a Loop Doctor verdict",
        "identifies missing budget and repeated-failure stop",
        "provides minimal repair and required evidence"
      ]
    },
    {
      "id": "risk-discovery",
      "prompt": "Use $loopright to scan a repository for dangerous or under-specified loops.",
      "expected_behaviors": [
        "uses risk discovery heuristics",
        "flags unbounded loops and broad retry catches",
        "labels findings as heuristics requiring review"
      ]
    },
    {
      "id": "cli-sarif-scan",
      "prompt": "Use the bundled LoopRight script to scan a repository and produce SARIF output for code scanning.",
      "expected_behaviors": [
        "uses skills/loopright/scripts/loopright.py scan",
        "emits SARIF without external dependencies",
        "preserves JSON or Markdown scan output as alternate formats"
      ]
    },
    {
      "id": "cli-loop-doctor",
      "prompt": "Use the bundled LoopRight script to diagnose a file or log and return a Loop Doctor report.",
      "expected_behaviors": [
        "uses skills/loopright/scripts/loopright.py doctor",
        "returns verdict, diagnosis, minimal repair, and required evidence",
        "treats scanner findings as heuristics requiring review"
      ]
    }
  ]
}
