# Agent Notes

Use `skills/loopright/SKILL.md` as the entry point. The skill must remain usable when only the `skills/loopright` folder is installed.

Do not make LoopRight depend on repository-level docs, examples, CI files, external services, or package managers.

